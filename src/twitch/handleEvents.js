export default function subAlert(
  channel,
  username,
  method,
  message,
  userstate
) {
  `🛠 subAlert 🛠 triggered: Channel = ${channel}. Username = ${username}. Method = ${method}. Message = ${message}. userstate = ${userstate}`;
  // Hook into OBS to show overlay
}
